---
description: Nexora/SGTD — reglas de negocio obligatorias (tareas, OT, métricas, log)
alwaysApply: true
---

# Reglas de negocio Nexora V4

## Fuente de verdad

- Estados, tipos y enums deben coincidir exactamente con el schema de BD (`web/CONTEXT/CONTEXT.md`)
- No crear valores nuevos en UI ni en lógica sin migración correspondiente

---

## 1. Modelo de tarea v1.1 (dos ejes)

**Detalle:** `web/CONTEXT/TAREA-MODEL.md`

### Eje 1 — `estado` (persistido, enum `estado_tarea`, migr. 040)

`pendiente` · `en_progreso` · `completada` · `cancelada`

Transiciones: `pendiente → en_progreso → completada`; cancelar desde `pendiente` o `en_progreso`.

**No escribir** `atrasada`, `reprogramada`, `bloqueada` en `estado`.

### Eje 2 — `situacion` (calculado en vista `tarea_activa`)

`atrasada` · `reprogramada` · `creada` · `null` (terminales)

- `atrasada`: planificada vencida con `estado IN ('pendiente','en_progreso')`
- `reprogramada`: `reprogramaciones > 0` (y no atrasada)
- La UI usa `claveVisualTarea()` (`lib/tableroEstado.ts`) para badges y conteos

---

## 2. Reprogramar

- RPC `sgtd_reprogramar_tarea_con_log`: cambia **fecha** + incrementa `reprogramaciones` + log
- **No cambia** `estado` (sigue `pendiente` salvo que el usuario haya iniciado)
- Justificación ≥10 caracteres obligatoria

---

## 3. Tipos de tarea

| Tipo | `fecha_planificada` | `semana_planificada` |
|------|--------------------|--------------------|
| `planificada` | obligatoria | obligatoria |
| `no_planificada` | = hoy | = semana actual |

**Eliminado:** `libre` — backlog en `nota_bitacora`.

---

## 4. Imprevistos

Toda tarea imprevista se crea con:
- `tipo = 'no_planificada'`
- `estado = 'completada'`
- `es_imprevisto = true`
- `fecha_planificada = hoy`

Se registran desde la columna del día actual en Mi Semana (`ZonaIncidencias`).

---

## 5. Log obligatorio con justificación

Registrar en `log_accion` con `justificacion` (≥10 caracteres obligatorio) al:
- Reprogramar fecha
- Cancelar tarea
- Eliminar tarea

Usar las RPCs atómicas correspondientes:
- `sgtd_reprogramar_tarea_con_log`
- `sgtd_eliminar_tarea_con_motivo`
- `sgtd_cambiar_estado_tarea` (cancelar con justificación)

**No permitir estas acciones sin justificación — validar en frontend Y en RPC.**

---

## 6. Métricas ponderadas

Pesos por prioridad: `critica = 4`, `alta = 3`, `media = 2`, `baja = 1`

```
% cumplimiento ponderado =
  Σ(peso de tareas completadas planificadas) /
  Σ(peso de tareas planificadas) × 100
```

Solo se cuentan tareas `planificadas` — no `no_planificadas`.

---

## 7. Flujo de Órdenes de Trabajo (OT)

**Detalle completo:** `.cursor/rules/sgtd-ot.mdc`

```
borrador → pendiente → aprobada → completada
              ↓ rechazada (jefe)
         cancelada (creador / jefe en BD)
```

- Sin estado `en_ejecucion` ni paso «Iniciar ejecución» (migración **036**).
- Número `OT-TI-XXXX` al **enviar** (`sgtd_enviar_ot`), idempotente; borrador sin número.
- Solo jefe aprueba/rechaza en `pendiente`.
- Cierre desde `aprobada`: receptor + DNI 8 dígitos (028/036).
- Sync tarea vinculada: migraciones **033** y **035**.

---

## 8. Objetivos

- Progreso = % tareas completadas / total tareas no canceladas vinculadas
- Jefe puede asignar responsable a cualquier miembro
- Miembro solo puede crear objetivos con `responsable_id = su propio id`
- Completar objetivo via RPC `sgtd_completar_objetivo`

---

## 9. Notas de bitácora (dentro de Mi Semana)

- Panel lateral (`PanelNotas`), visible en `xl+`
- Visibilidad: `todos`, `solo_jefe`, `privado`
- Pueden convertirse en tarea o evento (`convertida_en`)
- No hay página `/bitacora` dedicada en V4 — acceso solo desde Mi Semana

---

## 10. Permisos en edición de tareas

- Miembro: puede ver todas las tareas del equipo, solo modificar las propias
- Jefe: puede modificar cualquier tarea
- Tareas ajenas al miembro: solo lectura en UI + reforzado por RLS en BD
