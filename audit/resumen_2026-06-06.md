# SGTD — Resumen de sesión (refactor de Tarea + rediseño UX/UI)

Bitácora comprimida de lo trabajado. Fuentes de verdad: `TAREA-MODEL.md`,
`SISTEMA-DISENO.md` y `db/migrations/038–040`.

---

## 1. Base de datos — modelo de "dos ejes" (TERMINADO Y VERIFICADO ✓)

**Modelo.** La tarea tiene dos ejes:
- **Eje 1 — estado de ejecución** (persistido, enum 4 valores): `pendiente → en_progreso → completada`, + `cancelada`.
- **Eje 2 — situación** (calculada en la vista `tarea_activa`, no se guarda): `atrasada` / `reprogramada` / `creada`. `creada` no se pinta en UI.
- `tipo`: `planificada` / `no_planificada` (se eliminó `libre`). `prioridad`: se agregó `critica`.
- Reprogramar = `+1 reprogramaciones`, no cambia estado. Eliminar (soft-delete con motivo) ≠ cancelar.

**Migraciones (aplicadas en orden, sin big-bang):**
- **038** aditiva: agrega `critica`, `reprogramaciones`, `sla_atrasada_notificada_at`, recrea `tarea_activa` con `situacion` (lectura dual). Verificada.
- **039** lógica: quita triggers de "atrasada", neutraliza backfill, reescribe RPCs (cambiar_estado de 4 estados, reprogramar sin tocar estado), SLA por cron sobre situación derivada, y **remapea datos** (atrasada/reprogramada/bloqueada→pendiente; libre→planificada) en la misma migración. Verificada.
- **040** cirugía de enum: reduce `estado_tarea`(4)/`tipo_tarea`(2), rebind de 7 RPCs vivas, suelta/restaura CHECK + índice dependientes, borra RPCs bloquear/desbloquear + funciones muertas del SLA viejo + columna `sla_bloqueada_notificada_at`. Verificada.

**Estado final verificado en vivo:** enums reducidos (estado 4, tipo 2, prioridad con `critica`), 0 funciones muertas, solo trigger `tarea_updated_at`, `situacion` derivándose sola (≈38 atrasada / 12 creada / 2 reprogramada / 322 null).

**Pendiente menor:** borrar del repo el duplicado `040_rebind_funciones_vivos.sql`; desplegar los 2 filtros de frontend `situacion.eq.atrasada` (semana.ts / useTareas.ts) en el mismo deploy.

---

## 2. Sistema de diseño / UX (EN CURSO)

**Hallazgo del inventario:** el frontend ya tiene un sistema maduro (tokens `--mc-*` en `tokens.css`,
`estadoConfig.ts`, `Button.tsx`, lucide-react, React 19 + Vite + Tailwind3 con `theme.extend` vacío).
No hace falta inventar tokens: el trabajo es **ordenar y eliminar cruft**. Todo quedó en `SISTEMA-DISENO.md`.

**Decisiones cerradas:**
- **D1 — acento:** azul `#0064E0` único para todo lo interactivo; violeta `#534AB7` solo en el logo.
- **D2 — urgencia:** se elimina la escala horaria de 3 niveles; queda una sola señal **"Vence hoy"** en ámbar `#F59E0B` (token nuevo `--mc-state-vence-hoy-*`).
- **D3 — acciones de card:** primario (`Iniciar`/`Completar`) a casi todo el ancho (`flex:1`) + `⋮` fijo que abre popover con Reprogramar y Eliminar (texto rojo → ModalConfirmar).

**Lenguaje visual acordado:**
- Jerarquía **por superficie, no por peso**. Borde fino 0.5px parejo a 4 lados.
- **Sin barra lateral de prioridad**: punto antes del título solo en crítica (rojo) / alta (aro, opcional); media/baja nada.
- Estados en **pills de tinte suave** por los dos ejes; **sin amarillo** salvo el "Vence hoy".
- **Evento** = fondo más tenue + borde de color más marcado (recede frente a la tarea).
- **Completada** = atenuada y comprimida a check + título (sin tachado).
- **Acciones al hover** (`:hover`/`:focus-within`, ocultas con `display:none`→`flex`, NO opacity); en móvil siempre visibles. Card de **altura auto** (scroll en la columna, no en la card).

**Toolbar de Mi Semana:** quitar el switch "Mostrar completadas" (ya se comprimen); "Ver semana de"
pasa de estar tras el `⋯` a un **dropdown directo** en el toolbar, solo para jefe.

**Cruft a retirar:** `bloqueada` (tokens + icono Lock), escala horaria completa, barra de prioridad
`mc-semana-task-card__prio--*`, CSS DnD huérfano, `X` inline del Modal (usar lucide).

**Iteración de la card (resuelta paso a paso):** dedupe del "Vence hoy" → quitar barra lateral →
acciones contextuales → eliminar como icono → patrón primario+`⋮` (arregla la distorsión al cambiar
Iniciar↔Completar) → hover con `display` (arregla el hueco en reposo).

---

## 3. Próximos pasos

**Base de datos:** borrar 040 duplicado + aplicar los 2 filtros frontend (cierra el paso 5 de datos).

**UI (por Cursor, orden sugerido en `SISTEMA-DISENO.md`):**
1. Acento único + retirar cruft de color (bloqueada, escala horaria, barra prioridad).
2. Tipografía a tokens; estado como pill; lenguaje suave en la card; evento diferenciado.
3. Patrón de acciones primario+`⋮` con popover anclado; hover con `display`; altura auto.
4. Toolbar: fuera switch completadas; "ver semana de" dropdown jefe-only.
5. Primitivos: `Avatar`, menú/popover `⋮` reutilizable.
6. (Refactor mayor, al final) consolidar `TareaSemanaCard`/`TarjetaTarea`/`TaskItem` en una `TareaCard`.

**Microdecisión abierta:** aro para "alta" en la prioridad (opcional; hoy solo crítica lleva marca).

---

## Archivos de referencia
- `web/CONTEXT/TAREA-MODEL.md` — modelo de dos ejes (fuente de verdad del dominio).
- `web/CONTEXT/SISTEMA-DISENO.md` — sistema de diseño ordenado (tokens, colores, iconos, botones, componentes).
- `db/migrations/038–040` — refactor de base de datos aplicado.