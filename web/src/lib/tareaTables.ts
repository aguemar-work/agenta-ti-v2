/** Vista con RLS del invocador (migr. 034). Usar en lecturas operativas. */
export const TAREA_ACTIVA = 'tarea_activa' as const;

/** Tabla base — mutaciones e historial (incl. soft-deleted). */
export const TAREA = 'tarea' as const;
